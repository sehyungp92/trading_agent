# Trading Agent Workspace

Contract-first Python workspace for IBKR, K-stock, crypto, and assistant trading systems.

Canonical runtime evidence lives under `contracts/`, `backtests/baselines/`, and `deployments/*/generated/`.
Bot-local generated outputs and reference snapshots are migration inputs, not deployable package contents.
