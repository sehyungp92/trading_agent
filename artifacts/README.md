# Artifacts

This directory is reserved for local-only or externally stored artifacts.
Commit canonical indexes, schemas, promotion manifests, and generated deployment evidence in their dedicated root directories instead of storing large runtime outputs here.
